# 05 改訂・検証記録

2026-09-12。ボタンから学習ファイルを実行する版を廃止し、marimoの編集可能なコードセルへ改訂した。Windows実機と初学者の試読は未実施。

## 変更

- アプリ登録をrunからeditへ変更。初回は実際のセルを一巡し、以後の編集はlazy実行とした。初回のデータ取得・学習・保存・テストを本文冒頭で説明する。
- 実習用のボタン、subprocess実行、結果の切り替えUIを削除。`demo_import.py` を削除。
- `model.py` は実ファイルを読む行番号付き表示。その直下にimport・インスタンス作成・printの実行セルを配置。
- MNIST取得、画像表示、Tensor化、分割、DataLoader、損失関数、検証関数、学習ループ、作図、保存、復元、テストを全て表示されたコードで実行する。
- 過学習と分割の目的を練習問題の例から説明。テストに1〜2割を残すのは一例で、MNISTの公式分割を用いる理由、検証を別にする理由、未使用データとの違いを説明。
- クラス・継承・入力と出力の形・バッチ・エポック・損失・正解率・勾配・optimizer・学習率・状態保存の説明を見直した。
- データ保存先と重み保存先は使用するセルで指定。config.pyは補足CLIの学習設定のみとし、既定パスはpaths.pyへ移した。

## 現在のnotebookの実測

Apple M2、CPU 2スレッド、Python 3.13、torch 2.14.0、torchvision 0.29.0、marimo 0.24.0。

固定seed 42で公式訓練60,000枚の番号をrandpermし、先頭5,000を検証、次の40,000を訓練に使用。残り15,000は未使用。公式テスト10,000枚は最後に評価した。モデル784→128→10、ReLU、Adam、lr=0.001、batch 128、5 epoch。

`uv run python 05-pytorch/notebook.py` で、表示する実際のセルを初めから最後まで実行した。

| epoch | 訓練損失 | 検証損失 | 検証正解率 |
|---|---:|---:|---:|
| 1 | 0.4892 | 0.2939 | 91.42% |
| 2 | 0.2315 | 0.2179 | 93.26% |
| 3 | 0.1737 | 0.1883 | 94.04% |
| 4 | 0.1394 | 0.1565 | 95.04% |
| 5 | 0.1159 | 0.1367 | 95.80% |

学習・検証約2.0秒、採用epoch 5。保存した重みを新しいMLPへ読み込み、テスト正解率 **96.34%**、テスト損失0.1238。時間は初回取得・import・読解を含まず、各PCでの保証値ではない。

## 自動確認

- marimo check：セルの依存関係と重複定義に問題なし。
- `tests/test_pytorch_notebook.py`：実際のnotebookをネット接続なしの合成画像で一巡。訓練・テストの読み込み、分割の非重複、5 epochでの訓練損失の改善、検証損失での採用epoch、保存した値と復元したモデルの一致、テスト結果が復元した重みに対応し評価で重みを更新しないことを確認。保存先は一時フォルダ。
- `tests/test_pytorch.py` 7件：補足CLIの分割・指標・更新・保存復元・import・ファイル表示・結果の整合性を引き続き確認。
- `tests/test_portal.py` 8件：05が編集画面で開き初回出力が得られる設定、notebook自身のlazy設定、他章の登録・プロセス管理とポータルの経路を確認。

## 配布モデルと過去の測定

`assets/reference/` の96.43%と `assets/benchmarks.json` は、補足CLIの層化分割で生成した既存の記録。本編ではこの配布物を自分の実行結果として表示しない。

以前のCLIでは20k・30k・40k・55kを3 seedで検証し、40kで各seedが検証95%以上、55kとの差1ポイント以内になることを確認して設定を選んだ。今回、学習設定の再探索はしていない。読みやすいランダム分割へ変更した本編の96.34%は、CLI例の96.43%から0.09ポイントの差だった。

## アプリ画面での確認

更新したポータルを再起動し、05がmarimoの編集画面で開くことを確認。実ファイルの行番号付き表示と、その下のimportセル・MLPの層構造の出力を目視した。

初回の全セル実行でテスト96.34%まで完了。さらにShift+Enterでimportを実行すると、依存するセルが実行待ちになり、勝手に学習を繰り返さないことを確認した。続けて学習セルを実行し（学習・検証2.7秒）、保存・読み込み・テストの各コードセルを順に実行して、再び96.34%を確認した。初回画面実行の学習・検証は5.1秒で、実行方法や同時負荷でも所要時間が変わる。

## 部品の説明から全体コードへ再構成

損失を4枚で計算する例と、別モデルの重みを1回だけ更新する例を追加した。検証と重み選択の仕組みを説明した後、「部品をつなげて、実際に学習してみよう」でimport・データ取得・前処理・分割・DataLoader・モデル・損失・optimizer・検証関数・学習の全てを含む `train_mnist` の定義と呼び出しを同じセルに表示する。

前のセルの変数がない空の名前空間でも全体コードを実行できることを、独立したオフラインテストで確認。notebook全体の保存・復元・評価テストも成功。実際のMNISTで一巡し、5 epochの学習・検証2.9秒、復元したモデルのテスト正解率96.34%を再確認した。marimo checkも成功。
