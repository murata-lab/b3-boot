# AGENTS.md

この教材を編集するエージェント・人のための指針です。02（回帰とモデルの選び方）を作り直したときのレビューをもとにしています。
03〜05を直すときも、この指針に従ってください。

## 前提

- 読者は、機械学習に初めて触れる学部生です。プログラミングと大学初年級の数学は学んでいます。
- 読者は章ごとに、宿題として一人で読み進めます。質問できる人は近くにいません。**読むだけで納得できること**が最優先です。日数や1章あたりの時間を進め方の条件にしません。
- 提出物はありません。章末の確認問題は自己確認用です。
- 章は `marimo run` で開きます（01・05は `edit`）。学生はコードセルを見ません。

## 1. 読んで納得できる構成にする

- **図や操作の前に、なぜそれを見るのかを書く。** いきなりグラフを出さない。
  - 悪い例：「広さだけ」と「広さ＋駅徒歩」の予測と正解の散布図を急に並べ、後者のほうがよいと言う。
  - よい例：同じ広さでも家賃が違う部屋がある → 駅からの距離で色分けすると、直線の上下に分かれる → だから徒歩時間を特徴量に加える → 加えると直線がどう変わるか。
- **疑問 → 観察 → 説明**の順に書く。結論を先に言ってから図で追認させない。
- **新しい種類の図は、見慣れた形から段階的に導入する。** 初出の図の読み方（軸・点・線が何か）を一文で説明する。
- 一つの節で言いたいことは一つにする。節の最後に「次に何を考えるか」への橋渡しを一文置く。

## 2. 例は説明に合わせる

- **例が、説明したい現象を実際に、はっきり示しているかを数値と図で確認する。** 見せたい現象が弱い、または出ていない例は使わない。
  - 過学習を説明する節では、訓練MSEが下がり続け、検証MSEがはっきりU字に上がる訓練データを選ぶ。
  - 「訓練データを増やすと過学習しにくい」のような別の話題を、同じ図の切り替えに同居させない。別の図・別の小見出しで見せる。
- **図で点が重ならないようにする。** 乱数で作るデータは、近すぎる点を作り直す。
- 本文で「まだ分からない」「予測したい」と言う値のすぐ近くに、既知のデータ点を置かない（例：「28㎡の部屋の家賃は？」と問う図に28㎡の点がある）。
- 例は身近で、説明と一対一に対応するものにする。抽象的な「装置の出力」より、「家賃」「1日の気温」のように読者が想像できるものを選ぶ。
- 例のデータは説明用の合成データでよい。そのことは初出で一度だけ短く断る。

## 3. デモ（操作できる図）は少なく、意味のあるものだけにする

デモにするのは、次のどれかに当てはまるものだけです。1章に2〜3個までにします。

- 二つの見方の対応が分かる（例：データ上の直線 ↔ パラメータ平面上の1点）。
- 時間や設定による変化を、まとめて見ないと分からない（例：次数ごとの訓練MSEと検証MSE）。
- 直感に反する結果が出る（例：次数を上げると検証MSEが悪化する）。

式を読めば暗算で結果が分かるものはデモにしません（例：切片0のまま傾きを1→3に動かすとMSEはどうなるか）。静的な図と一文で十分です。

**図は多めに使う。** 文章で長く説明するより、静的な図を1枚置くほうがよい場面が多いです。

## 4. ボタンと操作部品

- **ボタンは、押す意味がある段階でだけ表示する。** 何も操作していないのに「最初に戻す」「リセット」を出さない。
- **ラベルだけで、押すと何が起きるか分かるようにする**（例：「損失の地形を表示」「谷底を表示」「実行」）。
- **答えを最初から見せない。** 「いろいろ試す → 全体を見る → 最適を確かめる」のように、段階的に表示する。
  現在どの段階かと、次に何をすればよいかを図の中に短く示す。
- 意味の違う操作を一つの図に詰め込まない。操作は1つの図に1〜2個まで。
- 学生が触らない marimo の UI（右上のメニュー、目次ミニマップなど）は CSS で隠す（`02-regression/notebook.css` を参照）。

## 5. 数値の見せ方

- 現在の値（MSE、w、b など）は、**図の凡例の一部として、図のすぐ上に並べる**。図から離れたツールバー風の読み出し行は作らない。
- 凡例は2系列以上なら必ず置く。色だけに頼らず、形（● と ○、実線と破線）でも区別する。
- 本文中の数値は、ハードコードせずに計算結果から埋め込む。データを変えても本文と図がずれないようにする。
- 本文の主張（「〜は必ず上がる」「〜に近い」など）が、実際の数値と合っているかを必ず確認する。

## 6. 問い

- **「予想」だけを押させる問いは置かない。** 本文やデモの途中に選択式の問いを挟まない。
- 問いは**章末の確認問題に5問**まとめる。選ぶとすぐに解説が出るようにする。
  長い章（05）は、区切りのよい節の終わりに一部を置いてよい（05は4節の終わりに2問、章末に3問）。
- 確認問題は、その章で扱った判断や区別を問う（用語の丸暗記ではなく、「この状況でどう判断するか」）。
- 明らかに外れと分かる選択肢で数を埋めない。よくある誤解を選択肢にする。

## 7. 文章

- 短く書く。1段落1つの主張。
- 操作説明は最小限にし、図の中の案内とボタンのラベルで伝える。本文で「〜を押すと〜が切り替わり〜」と長く書かない。
- 断り書き（「説明用であり保証するものではない」など）を何度も書かない。
- 用語は初出で太字にし、日本語の後に英語を括弧で添える（例：**過学習（overfitting）**）。
- 他の章とのつながり（「03で学ぶ勾配降下法」など）は、必要な箇所で一文だけ触れる。

## 8. コードを書き換えて試す欄

- **01で学ぶmarimoのセルと同じ見た目・操作にする。** 本文と同じ幅の枠、右上に丸い ▶、下に出力欄。01のセルにない「python」などのラベルや、独自のボタン・見出し（「実行」「結果」など）は作らない。
  - 作り方：`mo.ui.code_editor(...).form(submit_button_label="▶", submit_button_tooltip="実行", bordered=False)` を別のセルで作り（表示はしない）、
    出力のセルで `<div class="pycell">{フォーム}{出力欄}</div>` として表示する。見た目は `notebook.css` の「コード欄」の部分（02〜04に同じものがある）。
  - `run` モードでは、欄の外をクリックしたときの自動実行が当てにならないので、フォームで ▶ を押したときだけ実行する。▶ のほか `Ctrl+Enter`（Macは `⌘+Enter`）でも実行できる。
- **実行するまで出力欄は出さない。** 開いた時点で結果が出ていると、何が実行されたのか分からなくなる。「押すと結果が出ます」のような案内文も置かない。
- コードを書き換えただけでは結果を変えない。結果が変わるのは実行したときだけにする。
- 操作方法（▶ で実行すること）の説明は01で一度だけ行う。02以降の本文では繰り返さない。
- 課題は1〜3行の変更で試せるものにし、課題の数値目標（「MSEを0.1未満に」など）が実際に達成できるか確認する。

## 9. 図の実装（02の方式）

- 計算は Python（`notebook.py`）で行い、結果を `figure(name, data, caption)` で `figures.js` に渡して描く。
  `figure.html` が共通の見た目で、iframe は内容に合わせて高さが自動で変わる。
- 色は青（訓練・主系列）、橙（検証・比較）を基本にし、損失の大きさは白→青の単色の段階で表す。
- 図の中の数式記号（$ŷ$、$w$ など）は `$...$` で囲むと数式用の書体になる。
- 新しい図は、既存の部品（`chart`、`dots`、`legendItem`、`segmented`）を使って書く。
- 余白は `notebook.css` にまとめて決め、図や本文のセルごとに個別の余白を足さない。marimo はセルごとに上下 14.4px の余白を付け、それと相殺されるので、図（iframe）と別行の数式（`marimo-tex`）の前後の余白は `notebook.css` で調整する。

## 10. 変更後の確認

変更したら、必ず次を確認してください。

1. `uv run marimo check <章>/notebook.py` が通る。
2. `uv run marimo run <章>/notebook.py` で開き、PC幅（1280px 程度）とスマホ幅（390px 程度）で、すべての図を目で見る。
   点・ラベル・凡例の重なり、はみ出し、空白の大きすぎる箇所がないか。
3. すべてのボタン・切り替え・コード欄を実際に操作し、ブラウザのコンソールにエラーが出ない。
4. 本文中の数値と主張が、表示された図と一致している。
