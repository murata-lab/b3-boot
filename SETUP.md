# 環境の準備

[READMEへ戻る](README.md)

> 01〜05の教材とPython系列の指定ファイルは作成済みです。macOS・Windowsの新規環境での導入手順の通し確認は未完了です。

配属時に、Gitの導入と教材の取得（clone）までは一緒に行います。その後のこのページの手順は、自分で読みながら進めてください。

ここでは、Pythonの教材をブラウザで読むためにuvとmarimoを準備します。Pythonの基本は、準備後に01で学びます。

## 1. ターミナルを開く

macOSでは「ターミナル」、Windowsでは「PowerShell」を開きます。ターミナルは、文字でコマンドを入力する画面です。WindowsでWSLやGit Bashを使う必要はありません。

以降のコマンドはすべて、取得した `ml-bootcamp` フォルダで実行します。ここを「リポジトリのルート」と呼びます。PCのホームフォルダや、各章のフォルダとは別です。

保存先に合わせて、以下のパスを書き換えてください。空白を含むパスにも対応するため引用符で囲みます。

macOSの例：

```sh
cd "/Users/自分のユーザー名/code/ml-bootcamp"
```

Windowsの例（PowerShell）：

```powershell
cd "C:\Users\自分のユーザー名\code\ml-bootcamp"
```

現在の場所とファイルを確認します。どちらのOSでも、同じ画面で入力します。

```sh
pwd
ls
```

`pwd` が教材の保存先を示し、`ls` で `README.md`、`SETUP.md`、`01-python` が見えれば、作業場所は合っています。

## 2. uvを導入する

Pythonは教材のコードを書く言語で、実行にはPython本体と必要なライブラリを使います。uvはそれらをプロジェクトごとに管理する道具です。

[uvの公式インストール案内](https://docs.astral.sh/uv/getting-started/installation/)で、自分のOSの手順に従ってください。Python本体はuvに管理させます。

導入後はターミナルを開き直し、次を入力します。

```sh
uv --version
```

バージョン番号が表示されたら導入できています。手順1と同じように教材のルートへ移動してください。

## 3. 教材の環境を作る

marimoは、Pythonコードと説明・実行結果をブラウザで一緒に読める道具です。教材ルートで次を実行し、配布された依存関係を環境にそろえます。marimo・NumPy・Matplotlibはまとめて準備されます。

```sh
uv sync
```

`pyproject.toml` や `uv.lock` が見つからない場合は、教材の取得先と作業フォルダを確認してください。章ごとの初期化やライブラリ追加は不要です。

## 4. 教材の入口を開く

教材ルートの同じターミナルで実行します。

```sh
uv run python app.py
```

`uv run` は、このプロジェクトの環境で指定したプログラムを実行します。[uvとmarimoの連携](https://docs.astral.sh/uv/guides/integration/marimo/)

ブラウザに教材一覧が出れば準備完了です。「01 Python入門からはじめる」を押してください。自動で開かないときは、ターミナルに表示されたURLをブラウザで開きます。

左上の **☰** からサイドバーを開き、01〜05と教材一覧を行き来できます。起動コマンドの打ち直しは不要です。01と05はコードを編集・実行する画面、02〜04は本文・図・デモの閲覧・操作用の画面で開きます。05は初回に全セルを実行し、データ取得・学習・保存・テストの結果を表示します。

起動したターミナルは教材の実行に使われています。01のPythonコードの編集・実行はブラウザ内のセルで行い、結果もブラウザで見ます。

## 終了・再開するには

終了するときは、起動したターミナルで `Ctrl+C` を押します。入口と、そこから開いた教材がまとめて終了します。

再開するときはターミナルを開き、教材ルートへ移動して、同じ `uv run python app.py` を実行します。uvの再インストールやライブラリの追加を繰り返す必要はありません。

章を切り替えても、開いた教材の操作状態・スクロール位置は保たれます。ブラウザのページを再読み込み・終了するとリセットされます。01で保存したコードの変更はファイルに残ります。

ポートが使用中で起動できない場合は、`uv run python app.py --port 8001` のように別のポートを指定してください。

## うまく動かないとき

配属時の準備中なら、まずその場にいる先輩に声をかけてください。

自宅などで一人のときは、ChatGPTに相談してください。OS、今取り組んでいる手順、入力場所（ターミナル／marimo）、作業フォルダ、入力したコマンドやコード、エラー全文を伝え、「この教材はuvでPythonとライブラリを管理しています」と添えてください。

## 必要なときに読む：Gitと教材の更新

Gitは変更履歴を管理する道具、GitHubは教材や変更を共有する場所、リポジトリはファイルと履歴のまとまりです。cloneは教材を手元に取得する操作です。ここでは取得済みを前提とし、Gitの実習は必須にしません。

教材ルートのターミナルで、次の操作ができます。

| 操作 | 表示されるもの |
|---|---|
| `git status --short` | 変更されたファイル |
| `git diff` | まだステージしていない変更内容 |
| `git log -3 --oneline` | 最近3件の履歴 |

教材更新の案内があった場合は、marimoを終了し、`git status --short` を確認します。出力が空なら、`git pull --ff-only`、`uv sync` の順に実行します。変更がある場合は、表示内容を添えて研究室の先輩かChatGPTに相談してください。`uv sync` は記録された依存関係に環境をそろえる操作です。
